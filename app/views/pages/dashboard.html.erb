<div class="banner-show-pro">
      <div class="text-center">
        <h2>Dashboard</h2>
        <h4>Welcome <%= current_user.first_name %>, 🤿</h4>

            <% if current_user.pro? %>
              <div class="text-center my-3">
                <%= link_to "Add a new offer", new_offer_path, class: "btn btn-secondary" %>
                <%= link_to "Edit profile", edit_user_registration_path, class: "btn btn-secondary" %>
                <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "btn btn-secondary" %>
              </div>
            <% else %>
              <div class="text-center my-3">
                <%= link_to "Edit profile", edit_user_registration_path, class: "btn btn-secondary" %>
                <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "btn btn-secondary" %>
              </div>
            <%end %>

      </div>
</div>
<div class="container">
  <h3>Calendar:</h3>
  <%= month_calendar(events: @bookings, attribute: :start_at) do |date, bookings| %>
  <%= date %>

  <% bookings.each do |booking| %>
    <div class="meeting">
      <%= link_to "#{booking.user.first_name}", booking_path(booking), class: "btn btn-outline-success" %>
    </div>
  <% end %>
<% end %>
</div>
<%= render "bookings/dashboard"%>
